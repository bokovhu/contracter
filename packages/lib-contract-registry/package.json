{
    "name": "@degenhack/lib-contract-registry",
    "version": "0.0.1",
    "private": true,
    "main": "src/index.ts",
    "dependencies": {
        "@degenhack/contract-model": "*"
    },
    "devDependencies": {
        "esbuild": "*"
    },
    "scripts": {
        "build": "esbuild src/index.ts --format=iife --bundle --minify --sourcemap --platform=browser --outfile=index.js --global-name=DegenContractRegistry"
    }
}